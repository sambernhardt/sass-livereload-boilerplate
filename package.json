{
  "name": "sass-livereload-boilerplate",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sass": "sass --watch src/sass/style.scss:dist/css/style.css",
    "live-server": "live-server",
    "start": "concurrently \"live-server\" \"sass --watch src/sass/style.scss:dist/css/style.css\""
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "gulp": "^4.0.0"
  },
  "dependencies": {
    "concurrently": "^4.1.0"
  }
}
